{
  "scripts": {
    "back": "json-server-auth --delay 2000 --routes .backend/routes/routes.json --watch json-server.config.js --port 5000"
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "json-server-auth": "^2.1.0"
  }
}
